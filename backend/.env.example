# ─── Copy to .env on the server, then fill in real values ───
# docker-compose picks this up automatically when you run `docker compose up`

# MongoDB root credentials (mongo container will auto-create on first boot)
# ⚠️ Use ONLY alphanumeric characters — no @ : / # in the password,
#    otherwise the connection URI parsing will break.
MONGO_ROOT_USER=autohistorique_admin
MONGO_ROOT_PASS=change-me-to-a-long-random-string
MONGO_DB=autohistorique

# Alternative: if you MUST use special chars, set MONGO_URI directly
# with URL-encoded password (e.g. @ → %40, # → %23)
# MONGO_URI=mongodb://user:p%40ssword@mongo:27017/autohistorique?authSource=admin

# JWT — MUST be long and random in production (≥ 64 chars). Use:
#   openssl rand -base64 64
JWT_SECRET=change-me-openssl-rand-base64-64
JWT_EXPIRES_IN=8h

# Frontend origin (web). The Capacitor mobile app origins are always allowed.
FRONTEND_URL=https://autohistorique.ma
